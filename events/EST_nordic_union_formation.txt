add_namespace = EST_events

# News event for EST forming nordic union
news_event = {
	id = EST_events.3
	title = wtt_news.54.t
	desc = wtt_news.54.desc
	picture = GFX_news_event_generic_parliament

	is_triggered_only = yes
	major = yes

	option = {
		name = wtt_news.54.a
		trigger = {
			OR = {
				original_tag = DEN
				original_tag = SWE
				original_tag = NOR
				original_tag = FIN
				original_tag = ICE
				original_tag = EST
			}
			NOT = { has_war_with = FROM }
		}
	}
	option = {
		name = wtt_news.54.b
		trigger = {
			NOT = { original_tag = DEN }
			NOT = { original_tag = SWE }
			NOT = { original_tag = NOR }
			NOT = { original_tag = FIN }
			NOT = { original_tag = ICE }
			NOT = { original_tag = EST }
			NOT = { has_war_with = FROM }
		}
	}
	option = {
		name = wtt_news.53.c
		trigger = {
			has_war_with = FROM
		}
	}
}